# دليل التشغيل والصيانة - أكاديمية أحمد (Ahmed Academy)

## 🚀 دليل النشر والإنتاج (Production Deployment)

المنصة جاهزة للنشر على بيئة الإنتاج الخاصة بـ Replit أو أي مزود استضافة يدعم Next.js.

### 1. المتطلبات الأساسية
- قاعدة بيانات PostgreSQL (مفعلة تلقائياً في Replit).
- مفاتيح التشفير (Secrets):
  - `JWT_SECRET`: مفتاح عشوائي طويل لتشفير جلسات الدخول.
  - `JWT_REFRESH_SECRET`: مفتاح عشوائي لجلسات التحديث.
  - `NEXT_PUBLIC_APP_URL`: رابط الموقع النهائي (مثلاً: `https://ahmed-academy.replit.app`).

### 2. خطوات النشر (Publishing)
1. اضغط على زر **"Publish"** في أعلى واجهة Replit.
2. اختر نوع الاستضافة **"Autoscale"** للمواقع التي لا تتطلب حالة مستمرة في الذاكرة.
3. تأكد من إعداد أمر التشغيل: `npm start`.
4. تأكد من إعداد أمر البناء: `npm run build`.

---

## 🛠️ دليل الصيانة وإدارة المحتوى

### 1. إدارة المواد الدراسية (Materials)
- يتم إدارة المواد من خلال لوحة تحكم الأدمن (`/admin/dashboard`).
- البيانات مخزنة في جداول `Material` و `Chapter` و `Lesson`.
- **ملاحظة**: تأكد من تعيين القسم (Category) للمادة (Mechanical, Electrical, Civil) لتظهر في مكانها الصحيح في الفهرس الأكاديمي.

### 2. تحديث قاعدة البيانات (Database Schema)
عند إجراء تغييرات على ملف `prisma/schema.prisma`:
1. قم بتشغيل: `npx prisma db push`.
2. سيتم تحديث قاعدة البيانات فوراً دون الحاجة لملفات Migration معقدة (مناسب لبيئة Replit).

### 3. الحماية والأمان
- الموقع يستخدم `httpOnly cookies` لتخزين التوكن، مما يحمي من هجمات XSS.
- تأكد من عدم مشاركة `JWT_SECRET` أبداً.

### 4. الهوية البصرية (Theme)
- الألوان المعتمدة:
  - الكحلي الأكاديمي: `#1A2B4C`
  - البرتقالي التحذيري (للأزرار الهامة): `#FF4F00`
  - الخلفية: `#FFFFFF` (أبيض نقي)
- الخطوط: `Cairo` للنصوص العربية، `Bebas Neue` للعناوين الإنجليزية، `JetBrains Mono` للبيانات التقنية.

---

**أكاديمية أحمد للعلوم الهندسية المتقدمة**
*نحو تعليم هندسي رصين وتطبيق عملي متقن.*
